# No secrets required.
#
# The `pdf` tool only streams public HTTP and parses bytes locally —
# nothing to authenticate against. Kept as a placeholder so the
# tools/<name>/.env.example convention holds across every overlay tool.
