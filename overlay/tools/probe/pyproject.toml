[project]
name = "probe"
version = "0.1.0"
description = "Minimal liveness probe tool for the centaur-lab overlay."
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "python-dotenv>=1.0",
]

[tool.centaur]
module = "client.py"

[tool.uv]
package = false
